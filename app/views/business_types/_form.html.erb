<%= form_for @business_type, :html => { :class => "form-horizontal business_type" } do |f| %>

  <% if @business_type.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@business_type.errors.count, "error") %> prohibited this business_type from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @business_type.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :name, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= f.text_field :name, :class => 'form-control' %>
    </div>
    <%=f.error_span(:name) %>
  </div>
  <div class="form-group">
    <%= f.label :slug, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= f.text_field :slug, :class => 'form-control' %>
    </div>
    <%=f.error_span(:slug) %>
  </div>

  <div class="form-group">
    <div class="col-lg-offset-2 col-lg-10">
      <%= f.submit nil, :class => 'btn btn-primary' %>
      <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                business_types_path, :class => 'btn btn-default' %>
    </div>
  </div>

<% end %>
